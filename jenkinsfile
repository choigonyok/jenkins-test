node {
    stage('Clone repository') {
        checkout scm
        sh "echo '${BUILD_NUMBER}'"
    }

    stage('Build image') {
        app = docker.build("achoistic98/jenkins:$BUILD_NUMBER")
    }
}